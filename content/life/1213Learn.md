---
title: 1212Log
tags:
  - 日记
categories:
  - life
date: 2025-12-13
draft: "false"
summary: 周六应该也复习了几道题 但是没有什么困难的 所以没怎么记录
---
## Execution Log

- python格式化输出的又一些内容
	- `print(f"{'*'*(2*i-1):^{2*n-1}}")`  `:` 前的计算式需要使用 `()` 之后的格式限定使用 `{}` 进行计算
## Thinking Log

- 26 直接先加入 set 然后如果 不存在则再放到指针处，更新指针
- 80 直接和 i - k 处的元素 作比较即可 ，如果不相等则 必不可能有 k个
	- 我使用了计数，如果到达k个则不更新指针
	- 当 `write < k` 时直接写；当 `write >= k` 时，只要 `nums[read] != nums[write-k]` 就可以写入。
- Leetcode 11 完全没有头绪
	- 看了 题解之后 发现，这是一个从两边向中间收敛的过程
		- 如果移动短边则可能容积变大
		- 如果移动长边则必定容积变小
- 238. 除自身以外数组的乘积 完全没有头绪
	- 思路大概看了之后就明白了，但是具体写起来还是没写出来
		- 没有确定好数据结构的意义，使用ans先代表 i 左侧的所有元素的乘积
		- 再用一个变量代表 i 右侧所有元素的乘积
